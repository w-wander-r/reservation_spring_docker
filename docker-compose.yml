version: '3.8'
services:
  postgres:
    image: postgres:15
    container_name: reservation-db
    environment:
      POSTGRES_DB: reservation_db
      POSTGRES_USER: wander
      POSTGRES_PASSWORD: wander
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U wander -d reservation_db"]
      interval: 10s
      timeout: 5s
      retries: 5

volumes:
  postgres_data: